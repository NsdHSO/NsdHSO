name: Waka Readme

on:
  workflow_dispatch:
  schedule:
    # 
    - cron: "0 1,5,7,9,11,13,15,17,21,23 * * 1-5/2"

jobs:
  update-readme:
    name: Update this repo's README
    runs-on: ubuntu-latest
    steps:
      - uses: athul/waka-readme@master
        with:
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
